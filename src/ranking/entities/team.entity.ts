export class Team {
  ranking: number; //TODO: change to number
  name: string;
  points: number;
  rankingChange: string;
  //   lineUp: Player[];
}

type Player = {
  name: string;
  nickname: string;
  nationality: string;
};
